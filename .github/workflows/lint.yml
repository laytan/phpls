name: Lint

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  golangci-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v3.2.0
